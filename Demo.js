// Khai báo biến
let age = 233; 
const PI = 3.14; 
var oldVariable = "deprecated"; // không nên dùng (cũ)

// Các kiểu dữ liệu cơ bản
const name = "Phuc TranTran"; // string
const score = 100; // number
const isActive = true; // boolean
const person = { firstName: "Phuc", lastName: "TranTran" }; // object
const numbers = [1, 2, 3]; // array
let emptyValue = null;  //null: Giá trị rỗng (được gán chủ động)
let x; // undefined  Chưa được gán giá trị
let id = Symbol("unique_id"); //symbol (ES6): Giá trị duy nhất, dùng làm key cho object


// Toán tử số học
let sum = 10 + 5; 
let remainder = 10 % 3; //chia lấy dư

// Toán tử so sánh
console.log(10 > 5); // true
console.log(10 === "10"); // false (so sánh cả kiểu dữ liệu)

// Toán tử logic
console.log(true && false); // false (AND)
console.log(true || false); // true (OR)



